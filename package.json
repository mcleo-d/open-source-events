{
  "name": "open-source-events",
  "version": "0.0.1",
  "description": "Aggregator for UK open source conferences",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "mkdir -p dist && echo '<!doctype html><title>open-source-events</title><h1>Scaffolding</h1>' > dist/index.html",
    "preview": "npx serve dist"
  }
}
